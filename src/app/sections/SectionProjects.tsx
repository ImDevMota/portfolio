import NavbarProjects from "../_components/NavbarProjects";

export default function SectionProjects() {
  return (
    <section className="flex items-center justify-center mt-[25rem] mb-[10rem]">
      <NavbarProjects />
    </section>
  );
}
